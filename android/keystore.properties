# Keystore properties — placeholder template (RF-214)
# Copy this file, fill in your actual values, and keep it OUT of version control.
# The file should already be in .gitignore.

storeFile=../runform-release.keystore
storePassword=CHANGE_ME_STORE_PASSWORD
keyAlias=runform-release
keyPassword=CHANGE_ME_KEY_PASSWORD
